# Runtime dependencies for the E-560 Streamlit app.
# Versions are pinned to the exact set verified to work together.
# Install with:  pip install -r requirements.txt

pandas==3.0.5
numpy==2.5.1
openpyxl==3.1.5        # required by pandas.read_excel for the logbook file
matplotlib==3.11.1
streamlit==1.61.1
